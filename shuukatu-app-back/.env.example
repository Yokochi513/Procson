# Anthropic APIキー（ANTHROPIC_API_KEY でも api_key でも可。フロントの .env と同じ名前を流用できる）
api_key=sk-ant-xxxxxxxxxxxxxxxx

# CORS で許可するオリジン（カンマ区切り）。未設定なら全許可（開発用）。
# 本番は GitHub Pages のオリジン等に絞ることを推奨。
# ALLOWED_ORIGINS=https://<user>.github.io,http://localhost:3000
