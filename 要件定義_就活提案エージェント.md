# 要件定義書｜就活提案エージェント（MVP）

- 対象プロジェクト: 岡山県内の学生向け 就活お助けサイト（チーム名: UNO）
- 本書の担当範囲: Gemini API を使った「就活に寄り添う」相談エージェント
- 作成日: 2026-06-27
- マイルストーン: 6末に成果物収集・社員2名で統合 → 7/18 最終発表
- 本書のスコープ: **最小MVP（1機能を確実に動かす単位）**

---

## 1. 目的・コンセプト

就活生が抱える悩みや疑問に、Gemini API を介して**いつでも気軽に相談できるチャット**を提供する。
「正解を一方的に出す」より、**寄り添って一緒に考える**対話体験を最小構成で実現することをMVPのゴールとする。

> 既存機能との住み分け: 自己分析アンケート→おすすめ企業表示（秋田担当）とは独立した、**対話型の相談窓口**として位置づける。

---

## 2. スコープ

### 2.1 MVPに含む（やること）
- 就活相談チャット（テキスト対話）
- チャット開始時の簡易プロフィール入力（後述の3項目）
- 会話履歴の保存・復元（匿名ID単位）
- Gemini API への安全な接続（APIキーをサーバ側で秘匿）

### 2.2 MVPに含まない（やらないこと）
- ログイン／会員登録・認証
- アンケート結果や他メンバー成果物との連携
- 面接対策・ES生成・企業比較などの個別機能（将来拡張で別途）
- 本番デプロイ（URL公開）／独自ドメイン
- 応答のストリーミング表示
- 多言語対応・モバイルアプリ化

---

## 3. 想定ユーザー・利用シーン
- 主ユーザー: 岡山県内の就活中の学生（学部3年・修士1年 等）
- シーン例: 「ESの書き出しが分からない」「面接が不安」「業界の選び方を相談したい」を、思い立ったときに打ち込む

---

## 4. 機能要件（Functional Requirements）

| ID | 機能 | 内容 |
|----|------|------|
| FR-1 | 簡易プロフィール入力 | チャット開始時に **学年 / 志望業界・職種 / 現在の状況・悩み** を入力。任意項目（空でも開始可）。 |
| FR-2 | メッセージ送信 | ユーザーがテキストを送信できる。 |
| FR-3 | AI応答 | Gemini が就活相談として応答を返す。プロフィール＋会話履歴を文脈として踏まえる。 |
| FR-4 | 応答表示 | 応答は**生成完了後に一括表示**。生成中はローディング表示を出す。 |
| FR-5 | 履歴の保存 | 各メッセージ（ユーザー／AI）を SQLite に保存する。 |
| FR-6 | 履歴の復元 | 再アクセス時、匿名IDに紐づく過去の会話を読み込んで表示する。 |
| FR-7 | 匿名ID発行 | 初回アクセス時に匿名IDを生成し、ブラウザ（localStorage）に保存。以降これをキーに履歴を紐付ける。 |
| FR-8 | エラー処理 | API失敗・タイムアウト時はエラーメッセージを表示し、再送信できる。 |

### 4.1 会話の振る舞い（システムプロンプト方針）
- 就活相談を主目的としつつ、**就活以外の話題も自由に応じる**（雑談含め、寄り添う姿勢）。
- トーン: 丁寧かつ親しみやすい日本語。学生を励まし、具体的な次の一歩を提案する。
- 文脈活用: FR-1 の簡易プロフィールと直近の会話履歴を踏まえて回答する。
- 推奨（任意）: 深刻な精神的不調がうかがえる相談には、無理に踏み込まず**公的相談窓口の案内**を添える一文を最低限の安全配慮として入れることを推奨。

---

## 5. 画面・UI（最小）
- **単一のチャット画面**を1ページで構成。
  - 上部または初回モーダル: 簡易プロフィール入力（学年 / 志望業界・職種 / 現在の状況・悩み）
  - 中央: 会話表示エリア（ユーザー右／AI左 など）
  - 下部: 入力欄＋送信ボタン
  - 生成中: ローディングインジケータ
- フレームワーク: **Angular（HTML / TypeScript）**

---

## 6. システム構成・技術スタック

```
[ブラウザ / Angular]  --HTTP-->  [Node.js + Express (BE)]  --HTTPS-->  [Gemini API]
        |                              |
   localStorage(匿名ID)            [SQLite] 会話履歴
```

| 層 | 技術 | 備考 |
|----|------|------|
| フロント | Angular (HTML/TypeScript) | チーム標準 |
| バックエンド | Node.js + Express | `/api/chat` 等の最小プロキシ |
| AI | Gemini API | モデルは速度・コスト重視で `gemini-2.0-flash` 系を推奨（設定で変更可） |
| DB | SQLite | ファイル1つで完結。サーバ不要 |
| 実行 | ローカル起動 | `npm start` 等で localhost で動けば「完成」 |

> APIキーは **BE の環境変数（.env）** で管理し、フロント・リポジトリに含めない。

---

## 7. データモデル（SQLite）

```
messages
  id           INTEGER PK AUTOINCREMENT
  user_id      TEXT      -- 匿名ID
  role         TEXT      -- 'user' | 'assistant'
  content      TEXT
  created_at   DATETIME  -- 既定 CURRENT_TIMESTAMP

-- 任意: プロフィールを保存する場合
profiles
  user_id      TEXT PK   -- 匿名ID
  grade        TEXT      -- 学年
  industry     TEXT      -- 志望業界・職種
  situation    TEXT      -- 現在の状況・悩み
  updated_at   DATETIME
```

---

## 8. API仕様（最小）

### POST `/api/chat`
- リクエスト
```json
{
  "userId": "anon-xxxxxxxx",
  "message": "ESの書き出しが思いつきません",
  "profile": { "grade": "学部3年", "industry": "IT", "situation": "ESが書けない" }
}
```
- 処理: BEが `userId` の履歴を取得 → プロフィール＋履歴＋今回メッセージで Gemini 呼び出し → ユーザー発話とAI応答を `messages` に保存。
- レスポンス
```json
{ "reply": "まずは…という切り口で考えてみましょう。" }
```

### GET `/api/history?userId=anon-xxxxxxxx`
- レスポンス: 当該ユーザーのメッセージ配列（時系列）。

---

## 9. 非機能要件（最小限）
- 性能: 1往復あたり通常数秒以内（Gemini応答時間に依存）。生成中はローディング表示。
- コスト/濫用対策（推奨・任意）: 1メッセージの最大文字数制限、履歴は直近N件のみ文脈に含める。
- セキュリティ: APIキーはBEのみ保持。フロントへ露出させない。
- 互換: PCブラウザ（最新Chrome等）で動作すれば可。

---

## 10. 制約・前提
- 認証なし（匿名ID運用）。同一ブラウザ＝同一ユーザーとみなす。localStorageを消すと履歴は引き継がれない。
- 本番デプロイは社員の統合フェーズで対応。本MVPはローカル起動で完成とする。
- 個人情報は収集しない（匿名ID＋任意プロフィールのみ）。

---

## 11. 受け入れ基準（完成の定義）
1. ローカルで Angular フロントと Express BE が起動する。
2. 初回アクセスで匿名IDが発行・保存される。
3. 簡易プロフィールを入力（空でも可）してチャットを開始できる。
4. メッセージを送ると、文脈を踏まえた Gemini の応答が一括表示される。
5. 送受信したメッセージが SQLite に保存される。
6. ページを再読み込みしても、匿名IDに紐づく会話履歴が復元される。
7. API失敗時にエラー表示され、再送信できる。

---

## 12. 将来拡張（MVP外・メモ）
- アンケート結果／おすすめ企業との連携（パーソナライズ強化）
- 面接対策（模擬面接）・ESドラフト作成・企業比較・面接質問集
- ストリーミング表示、ログイン、本番デプロイ
- 岡山の地元企業データを文脈に組み込む（地域特化の寄り添い）
```
