# シェアルバム バックエンド 環境変数サンプル
# 実運用では .env にコピーして値を設定（.env はコミットしない）
PORT=3000
# JWT 署名鍵（推測困難なランダム文字列に変更すること）
JWT_SECRET=change-me-to-a-random-secret
# 1アルバムあたりの上限枚数（NFR-3 濫用対策）
MAX_PHOTOS_PER_ALBUM=500
# 1枚あたりの最大サイズ(バイト) 既定 15MB
MAX_PHOTO_SIZE=15728640
