# 基本設計書｜就活提案エージェント（MVP）

## 0. 文書情報

| 項目 | 内容 |
|------|------|
| 文書名 | 基本設計書（就活相談チャットエージェント MVP） |
| 版数 | 1.0 |
| 作成日 | 2026-06-30 |
| 元となる要件定義書 | 要件定義_就活提案エージェント.md（MVP） |
| 要件IDの扱い | 機能要件（FR-1〜FR-8）は要件定義書のIDをそのまま使用。非機能要件・制約は**本設計書で採番**（NFR-x / CON-x）。 |
| 関連文書 | （なし） |

> 採番ルール: 要件定義書「9. 非機能要件」を `NFR-1`〜 として、「10. 制約・前提」を `CON-1`〜 として本設計書で採番した。会話の振る舞い方針（要件定義書 4.1）は FR-3 の一部として扱う。

---

## 1. システム概要

- **目的**: 岡山県内の就活生が、悩みや疑問を**いつでも気軽にテキストで相談できるチャット**を提供する。「正解を一方的に出す」のではなく、プロフィールと会話履歴を踏まえて**寄り添って一緒に考える**対話体験を最小構成で実現する。
- **設計方針**:
  - **最小MVP優先**: 1機能（就活相談チャット）を確実にローカルで動かすことを最優先とし、認証・デプロイ・他機能連携は持ち込まない。
  - **APIキーのサーバ側秘匿**: Gemini API キーはバックエンド（BE）のみが保持し、フロント・リポジトリに露出させない（BE = 最小プロキシ）。
  - **匿名ID運用**: ログインを設けず、ブラウザ localStorage の匿名IDをキーに履歴を紐付ける。
- **スコープ（設計対象）**: 就活相談チャット（FR-2, FR-3, FR-4）／簡易プロフィール入力（FR-1）／会話履歴の保存・復元（FR-5, FR-6）／匿名ID発行（FR-7）／Gemini API への安全な接続（NFR-3）／エラー処理（FR-8）。
- **スコープ外**（要件定義書 2.2 をそのまま反映）:
  - ログイン／会員登録・認証
  - アンケート結果や他メンバー成果物との連携
  - 面接対策・ES生成・企業比較などの個別機能
  - 本番デプロイ（URL公開）／独自ドメイン
  - 応答のストリーミング表示
  - 多言語対応・モバイルアプリ化

---

## 2. システムアーキテクチャ

### 2.1 構成図

```
┌──────────────────────────┐        ┌──────────────────────────┐        ┌──────────────┐
│   ブラウザ / Angular     │        │  Node.js + Express (BE)  │        │  Gemini API  │
│  ───────────────────     │  HTTP  │  ──────────────────────  │ HTTPS  │ (gemini-2.0- │
│  チャット画面            │ ─────► │  POST /api/chat          │ ─────► │  flash 系)   │
│  プロフィール入力        │        │  GET  /api/history       │ ◄───── │              │
│  匿名ID (localStorage)   │ ◄───── │  APIキー(.env)で秘匿     │        └──────────────┘
└──────────────────────────┘        │            │             │
                                     │            ▼             │
                                     │      ┌───────────┐       │
                                     │      │  SQLite   │       │
                                     │      │ messages  │       │
                                     │      │ profiles  │       │
                                     │      └───────────┘       │
                                     └──────────────────────────┘
```

データフロー（1往復）:
1. ブラウザが匿名ID・メッセージ・プロフィールを `POST /api/chat` に送信。
2. BE が `userId` の履歴を SQLite から取得。
3. BE が「プロフィール＋直近履歴＋今回メッセージ」で Gemini API を呼び出す（APIキーは .env）。
4. BE がユーザー発話とAI応答を `messages` に保存し、`reply` を返却。
5. ブラウザが応答を一括表示。

### 2.2 技術スタック

| 層 | 技術 | 責務 | 対応要件 |
|----|------|------|----------|
| フロント | Angular (HTML/TypeScript) | チャットUI描画、プロフィール入力、匿名ID管理、BEへのHTTP通信、ローディング/エラー表示 | FR-1, FR-2, FR-4, FR-6, FR-7, FR-8, NFR-4 |
| バックエンド | Node.js + Express | `/api/chat`・`/api/history` の最小プロキシ、Gemini呼び出し、履歴永続化、APIキー秘匿 | FR-3, FR-5, FR-6, FR-8, NFR-2, NFR-3 |
| AI | Gemini API（`gemini-2.0-flash` 系、設定で変更可） | 就活相談としての応答生成（文脈反映） | FR-3 |
| DB | SQLite（ファイル1つ） | 会話履歴（messages）・プロフィール（profiles）の永続化 | FR-5, FR-6 |
| 実行環境 | ローカル起動（localhost） | 開発・動作確認環境（`npm start` 等） | CON-2 |

### 2.3 コンポーネントと責務

| コンポーネント | 責務 | 依存先 | 対応要件 |
|----------------|------|--------|----------|
| ChatComponent（FE） | 会話表示・入力欄・送信ボタン・ローディング/エラー表示の描画と操作 | ChatService | FR-2, FR-4, FR-8 |
| ProfileInput（FE） | 初回モーダル/上部での簡易プロフィール（学年/志望業界・職種/状況・悩み）入力（任意） | ChatService | FR-1 |
| ChatService（FE） | 匿名IDの発行/取得（localStorage）、`/api/chat`・`/api/history` 呼び出し | BE API | FR-6, FR-7 |
| chat ルータ（BE） | `POST /api/chat` 受信、履歴取得→Gemini呼び出し→保存→応答返却 | GeminiClient, MessageRepository | FR-3, FR-5 |
| history ルータ（BE） | `GET /api/history` で時系列メッセージ配列を返却 | MessageRepository | FR-6 |
| GeminiClient（BE） | システムプロンプト・プロフィール・履歴の組み立てと Gemini API 呼び出し、タイムアウト/失敗ハンドリング | Gemini API（.env のキー） | FR-3, FR-8, NFR-3 |
| MessageRepository（BE） | messages/profiles テーブルへの保存・取得 | SQLite | FR-5, FR-6 |

> BE は「就活相談ロジックの中核（プロンプト組み立て）＋永続化＋キー秘匿」を担い、認証・他システム連携は**担わない**（スコープ外）。

---

## 3. 機能設計

### 3.1 機能一覧

| 機能ID | 機能名 | 概要 | 対応要件 |
|--------|--------|------|----------|
| FR-1 | 簡易プロフィール入力 | 開始時に学年/志望業界・職種/状況・悩みを入力（任意、空でも開始可） | FR-1 |
| FR-2 | メッセージ送信 | ユーザーがテキストを送信 | FR-2 |
| FR-3 | AI応答 | Gemini がプロフィール＋履歴を文脈に就活相談として応答 | FR-3 |
| FR-4 | 応答表示 | 生成完了後に一括表示。生成中はローディング表示 | FR-4 |
| FR-5 | 履歴の保存 | 各メッセージを SQLite に保存 | FR-5 |
| FR-6 | 履歴の復元 | 再アクセス時、匿名IDの過去会話を読み込み表示 | FR-6 |
| FR-7 | 匿名ID発行 | 初回アクセス時に匿名IDを生成し localStorage に保存 | FR-7 |
| FR-8 | エラー処理 | API失敗・タイムアウト時にエラー表示し再送信可 | FR-8 |

### 3.2 機能詳細

#### FR-1: 簡易プロフィール入力 （対応要件: FR-1）

- **トリガー / 利用シーン**: チャット開始時（初回モーダル、または画面上部の入力領域）。
- **入力**: 学年（`grade`）/ 志望業界・職種（`industry`）/ 現在の状況・悩み（`situation`）。**いずれも任意**（空でもチャット開始可）。
- **処理概要**: 入力値をフロントで保持し、`POST /api/chat` の `profile` として毎回送信する。profiles テーブルへの保存は任意（前提P-3、10章参照）。
- **出力 / 結果**: プロフィールが Gemini 呼び出しの文脈に反映される。
- **主な分岐・例外**: 全項目空 → プロフィールなしで開始（`profile` は空オブジェクト/未設定可）。

#### FR-2: メッセージ送信 （対応要件: FR-2）

- **トリガー / 利用シーン**: 画面下部の入力欄に文字を入力し送信ボタン押下（またはEnter送信）。
- **入力**: テキスト1件（最大 `MAX_MESSAGE_LEN` 文字、前提P-1。既定 2000 文字）。
- **処理概要**: 入力検証（空文字/超過文字のチェック）→ `POST /api/chat` を呼び出す → 送信中は入力欄・ボタンを無効化しローディング表示（FR-4）。
- **出力 / 結果**: 会話表示エリアにユーザー発話を即時表示し、AI応答を待機。
- **主な分岐・例外**: 空文字は送信不可。文字数超過時は送信前に警告（8章 E-1）。

#### FR-3: AI応答 （対応要件: FR-3）

- **トリガー / 利用シーン**: `POST /api/chat` 受信時（BE）。
- **入力**: `userId`, `message`, `profile`。
- **処理概要**:
  1. `MessageRepository` で `userId` の履歴を取得（直近 `HISTORY_N` 件のみ文脈に含める。前提P-2、既定 20 件）。
  2. `GeminiClient` がシステムプロンプト＋プロフィール＋直近履歴＋今回メッセージを組み立て Gemini API を呼び出す。
  3. 応答（`reply`）を受領。
  4. ユーザー発話とAI応答を `messages` に保存（FR-5）。
  5. `reply` を返却。
  - **会話の振る舞い方針（要件定義書 4.1）**:
    - 就活相談を主目的としつつ、就活以外の話題（雑談含む）にも自由に応じる。
    - トーン: 丁寧かつ親しみやすい日本語。学生を励まし、具体的な次の一歩を提案する。
    - 深刻な精神的不調がうかがえる場合は、無理に踏み込まず**公的相談窓口の案内**を一文添える（安全配慮・推奨）。
- **出力 / 結果**: `{ "reply": "..." }`。
- **主な分岐・例外**: Gemini 失敗/タイムアウト → エラー応答（8章 E-2）。この場合メッセージは保存しない（または保存ポリシーは前提P-4、10章参照）。

#### FR-4: 応答表示 （対応要件: FR-4）

- **トリガー / 利用シーン**: メッセージ送信後の応答待ち〜受領。
- **入力**: BE からの `reply`。
- **処理概要**: 送信直後にローディングインジケータを表示 → `reply` 受領後に**一括表示**（ストリーミングしない）。
- **出力 / 結果**: AI応答を会話表示エリア（AI左 等）に追加表示。
- **主な分岐・例外**: エラー時はローディングを解除しエラーメッセージ表示（FR-8）。

#### FR-5: 履歴の保存 （対応要件: FR-5）

- **トリガー / 利用シーン**: `POST /api/chat` 処理内、Gemini応答受領後。
- **入力**: ユーザー発話（`role='user'`）、AI応答（`role='assistant'`）。
- **処理概要**: `messages` テーブルに2レコード（user / assistant）を `user_id` 付きで INSERT。`created_at` は CURRENT_TIMESTAMP。
- **出力 / 結果**: 永続化完了。
- **主な分岐・例外**: DB書き込み失敗 → 8章 E-3。

#### FR-6: 履歴の復元 （対応要件: FR-6）

- **トリガー / 利用シーン**: ページ再読み込み/再アクセス時（FE 初期化）。
- **入力**: localStorage の匿名ID。
- **処理概要**: `GET /api/history?userId=...` を呼び出し、時系列のメッセージ配列を取得して会話表示エリアに復元描画。
- **出力 / 結果**: 過去会話が画面に再表示される。
- **主な分岐・例外**: 該当履歴なし → 空配列（新規会話として開始）。

#### FR-7: 匿名ID発行 （対応要件: FR-7）

- **トリガー / 利用シーン**: 初回アクセス時（FE 初期化、localStorage に匿名IDが無い場合）。
- **入力**: なし。
- **処理概要**: 衝突しにくいID（例: `anon-` ＋ UUID/ランダム文字列）を生成し localStorage に保存。以降はこれをキーに利用。
- **出力 / 結果**: 匿名IDが永続化され、以降の API 呼び出しに付与される。
- **主な分岐・例外**: localStorage を消去 → 新規IDが発行され過去履歴は引き継がれない（CON-1）。

#### FR-8: エラー処理 （対応要件: FR-8）

- **トリガー / 利用シーン**: API失敗・タイムアウト時。
- **入力**: エラー応答 / 通信エラー。
- **処理概要**: ローディング解除 → エラーメッセージ表示 → 直前の送信内容を保持し**再送信**を可能にする。
- **出力 / 結果**: ユーザーがワンアクションで再送信できる。
- **主な分岐・例外**: 詳細は 8章。

---

## 4. 画面設計 / UI設計

### 4.1 画面一覧・画面遷移

| 画面ID | 画面名 | 概要 | 対応要件 |
|--------|--------|------|----------|
| SC-1 | チャット画面（単一ページ） | プロフィール入力＋会話表示＋入力欄を1ページに集約 | FR-1, FR-2, FR-4, FR-6, FR-8 |

```
[初回アクセス] ─► 匿名ID発行(FR-7) ─► 履歴復元(FR-6) ─► SC-1 表示
     │
     └─(プロフィール未入力)─► プロフィール入力(任意/FR-1) ──┐
                                                            ▼
[SC-1] メッセージ入力 ─► 送信(FR-2) ─► ローディング(FR-4) ─► 応答表示(FR-4)
     ▲                                                │
     └──────────── エラー時: エラー表示＋再送(FR-8) ◄─┘
```

### 4.2 画面詳細

#### SC-1: チャット画面（単一ページ）

| 領域 | 構成要素 | 表示項目 / 操作 | 状態 | 対応要件 |
|------|----------|-----------------|------|----------|
| 上部 / 初回モーダル | プロフィール入力 | 学年・志望業界・職種・状況/悩みの入力欄（任意）、開始ボタン | 入力中/未入力でも開始可 | FR-1 |
| 中央 | 会話表示エリア | ユーザー発話（右）/ AI応答（左）の吹き出しを時系列表示 | 通常/ローディング（AI生成中インジケータ）/エラー（エラーメッセージ＋再送ボタン） | FR-4, FR-6, FR-8 |
| 下部 | 入力フォーム | テキスト入力欄＋送信ボタン | 送信中は無効化 | FR-2, FR-4 |

- **ローディング状態**: 送信〜応答受領まで、AI側にローディングインジケータを表示し、入力欄・送信ボタンを無効化（FR-4, NFR-1）。
- **エラー状態**: 直前の送信内容を保持し、エラーメッセージと再送ボタンを表示（FR-8）。
- **対応環境**: PCブラウザ（最新Chrome等）で表示・操作できること（NFR-4）。

---

## 5. 外部インターフェース設計

### 5.1 API一覧

| メソッド | パス | 概要 | 対応要件 |
|----------|------|------|----------|
| POST | `/api/chat` | メッセージ送信→Gemini応答→履歴保存→応答返却 | FR-3, FR-5 |
| GET | `/api/history` | 匿名IDの会話履歴（時系列）を返却 | FR-6 |

### 5.2 API詳細

#### POST `/api/chat` （対応要件: FR-3, FR-5）

- **概要 / 処理**: `userId` の履歴を取得し、プロフィール＋直近履歴＋今回メッセージで Gemini を呼び出す。ユーザー発話とAI応答を `messages` に保存し、AI応答を返す。
- **リクエスト**（`Content-Type: application/json`）:

| フィールド | 型 | 必須 | 説明 |
|------------|----|------|------|
| `userId` | string | 必須 | 匿名ID（例: `anon-xxxxxxxx`） |
| `message` | string | 必須 | ユーザー発話（最大 `MAX_MESSAGE_LEN` 文字） |
| `profile` | object | 任意 | `{ grade, industry, situation }`（各 string・任意） |

```json
{
  "userId": "anon-xxxxxxxx",
  "message": "ESの書き出しが思いつきません",
  "profile": { "grade": "学部3年", "industry": "IT", "situation": "ESが書けない" }
}
```

- **レスポンス（正常系・200）**:

```json
{ "reply": "まずは…という切り口で考えてみましょう。" }
```

- **エラーレスポンス**: バリデーション違反（400）、Gemini失敗/タイムアウト（502/504）、DB保存失敗（500）。形式は下記共通エラー（8章参照）。

```json
{ "error": { "code": "GEMINI_TIMEOUT", "message": "AIの応答取得に失敗しました。再送信してください。" } }
```

#### GET `/api/history` （対応要件: FR-6）

- **概要 / 処理**: `userId` に紐づく `messages` を時系列（`created_at` 昇順）で返す。
- **リクエスト**: クエリパラメータ `userId`（string・必須）。
- **レスポンス（正常系・200）**:

```json
{
  "messages": [
    { "role": "user", "content": "ESが書けない", "createdAt": "2026-06-30T10:00:00Z" },
    { "role": "assistant", "content": "まずは…", "createdAt": "2026-06-30T10:00:03Z" }
  ]
}
```

- **エラーレスポンス**: `userId` 欠落（400）、DB読み込み失敗（500）。

### 5.3 外部システム連携

| 連携先 | 用途 | 方式 / 認証 | タイムアウト・リトライ方針 | 対応要件 |
|--------|------|-------------|----------------------------|----------|
| Gemini API | 就活相談の応答生成 | HTTPS / APIキー（BEの `.env`、フロント非公開） | タイムアウト既定 `GEMINI_TIMEOUT_MS`（前提P-5、既定 30秒）。**自動リトライはしない**（失敗時はユーザー操作で再送信＝FR-8）。 | FR-3, NFR-3 |

---

## 6. データ設計

### 6.1 論理データモデル

```
profiles (匿名ID単位の任意プロフィール)
   user_id ──────────┐
                      │ 1
                      │
                      │ N
messages (会話ログ)  ─┘
   user_id, role, content, created_at
```

- `messages` と `profiles` は `user_id`（匿名ID）で論理的に関連。外部キー制約は必須としない（MVP・前提P-3）。

### 6.2 物理データモデル / テーブル定義

#### テーブル: `messages` （対応要件: FR-5, FR-6）

| カラム | 型 | 制約 | 説明 |
|--------|----|------|------|
| `id` | INTEGER | PK, AUTOINCREMENT | レコードID |
| `user_id` | TEXT | NOT NULL | 匿名ID。検索キー（インデックス推奨） |
| `role` | TEXT | NOT NULL（`'user'` \| `'assistant'`） | 発話主体 |
| `content` | TEXT | NOT NULL | 本文 |
| `created_at` | DATETIME | 既定 CURRENT_TIMESTAMP | 作成日時（時系列復元に使用） |

> 取得性能のため `user_id`（必要なら `(user_id, created_at)`）にインデックスを付与することを推奨。

#### テーブル: `profiles`（任意） （対応要件: FR-1）

| カラム | 型 | 制約 | 説明 |
|--------|----|------|------|
| `user_id` | TEXT | PK | 匿名ID |
| `grade` | TEXT | NULL可 | 学年 |
| `industry` | TEXT | NULL可 | 志望業界・職種 |
| `situation` | TEXT | NULL可 | 現在の状況・悩み |
| `updated_at` | DATETIME | — | 更新日時 |

> 要件定義書では profiles は「任意」。MVPではプロフィールを毎回リクエストで送る方式でも成立するため、サーバ保存の採否は前提P-3として10章に記録する。

---

## 7. 非機能設計

| 区分 | 要件 / 目標値 | 設計上の実現方法 | 対応要件 |
|------|---------------|------------------|----------|
| 性能 | 1往復あたり通常数秒以内（Gemini応答時間に依存）。体感悪化を防ぐ。 | 生成中はローディング表示（FR-4）で待ち時間を可視化。直近 `HISTORY_N` 件のみを文脈に含めプロンプト肥大を抑制。 | NFR-1 |
| コスト / 濫用対策 | 1メッセージの最大文字数制限、履歴は直近N件のみ文脈化（推奨・任意） | FE/BE双方で `MAX_MESSAGE_LEN`（既定2000）超過を拒否。BEで履歴を直近 `HISTORY_N`（既定20）件に制限。 | NFR-2 |
| セキュリティ | APIキーはBEのみ保持。フロントへ露出させない。個人情報は収集しない。 | キーは BE の `.env` で管理しリポジトリ・フロントに含めない。BE が Gemini への唯一の経路（プロキシ）。匿名ID＋任意プロフィールのみ扱う。 | NFR-3, CON-3 |
| 互換性 | PCブラウザ（最新Chrome等）で動作 | Angular 標準機能で実装し、最新Chromeでの表示・操作を確認対象とする。 | NFR-4 |
| 可用性 / 運用 | ローカル起動で完成（本番デプロイ・URL公開は対象外） | `npm start` 等で localhost 起動。SQLite はファイル1つで完結しサーバ不要。 | CON-2 |

---

## 8. エラー処理・例外設計

| エラーID | エラー種別 | 発生条件 | システムの振る舞い | ユーザーへの通知 | 対応要件 |
|----------|------------|----------|--------------------|------------------|----------|
| E-1 | 入力バリデーション | 空メッセージ / `MAX_MESSAGE_LEN` 超過 | 送信を行わない（FE）/ 400返却（BE） | 入力欄付近に警告表示。送信不可。 | FR-2, NFR-2 |
| E-2 | Gemini失敗 / タイムアウト | API応答エラー / `GEMINI_TIMEOUT_MS` 超過 | リトライせず502/504でエラー応答。ログ記録。 | ローディング解除＋エラーメッセージ＋**再送ボタン**（直前入力を保持） | FR-8 |
| E-3 | DB保存/読込失敗 | SQLite書き込み/読込エラー | 500返却。ログ記録。 | エラーメッセージ表示。再送/再読込を案内。 | FR-5, FR-6, FR-8 |
| E-4 | パラメータ不足 | `userId` 欠落 等 | 400返却 | エラーメッセージ表示。 | FR-6, FR-8 |
| E-5 | 通信エラー | ネットワーク断 / BE未起動 | FEがタイムアウト/失敗を検知 | エラーメッセージ＋再送ボタン | FR-8 |

- 共通エラー形式: `{ "error": { "code": "<識別子>", "message": "<日本語メッセージ>" } }`

---

## 9. 要件トレーサビリティマトリクス

| 要件ID | 要件概要 | 対応する設計箇所（章番号・項目） | カバレッジ |
|--------|----------|----------------------------------|------------|
| FR-1 | 簡易プロフィール入力（任意） | 2.3, 3.2(FR-1), 4.2, 5.2(/api/chat), 6.2(profiles) | ✅ |
| FR-2 | メッセージ送信 | 2.3, 3.2(FR-2), 4.2, 8(E-1) | ✅ |
| FR-3 | AI応答（文脈反映） | 2.3, 3.2(FR-3), 5.2(/api/chat), 5.3 | ✅ |
| FR-4 | 応答表示（一括/ローディング） | 3.2(FR-4), 4.2, 7(NFR-1) | ✅ |
| FR-5 | 履歴の保存 | 2.3, 3.2(FR-5), 5.2(/api/chat), 6.2(messages) | ✅ |
| FR-6 | 履歴の復元 | 2.3, 3.2(FR-6), 4.1, 5.2(/api/history), 6.2(messages) | ✅ |
| FR-7 | 匿名ID発行 | 2.3, 3.2(FR-7), 4.1 | ✅ |
| FR-8 | エラー処理（再送信） | 3.2(FR-8), 4.2, 8 | ✅ |
| NFR-1 | 性能（数秒以内/ローディング） | 4.2, 7 | ✅ |
| NFR-2 | コスト/濫用対策（文字数制限・直近N件） | 3.2(FR-2/FR-3), 7, 8(E-1) | ✅（前提P-1,P-2で数値確定） |
| NFR-3 | セキュリティ（キーBEのみ・個人情報非収集） | 2.1, 2.3, 5.3, 7 | ✅ |
| NFR-4 | 互換（最新Chrome等） | 2.2, 4.2, 7 | ✅ |
| CON-1 | 認証なし・匿名ID運用 | 1, 3.2(FR-7), 10 | ✅ |
| CON-2 | ローカル起動で完成 | 2.2, 7 | ✅ |
| CON-3 | 個人情報非収集 | 7, 10 | ✅ |

- カバー: **15 / 全 15 件**
- 未カバー / 未決: **0 件**（数値未定だった NFR-2 等は前提P-1〜P-5で設計上のデフォルトを確定。10章参照）

---

## 10. 未決事項・前提・課題

| # | 種別 | 内容 | 影響範囲 | 対応方針 |
|---|------|------|----------|----------|
| P-1 | 前提 | `MAX_MESSAGE_LEN`（1メッセージ最大文字数）を**既定2000文字**と設定。要件では「推奨・任意」。 | FR-2, NFR-2, E-1 | 設定値として実装。チームレビューで変更可。 |
| P-2 | 前提 | 文脈に含める履歴を**直近20件（HISTORY_N=20）**と設定。要件では「直近N件」とのみ記載。 | FR-3, NFR-2 | 設定値として実装。コスト/品質を見て調整。 |
| P-3 | 前提 | profiles テーブルへのサーバ保存は**任意**。MVPではプロフィールを毎リクエストで送る方式でも成立するため、保存採否は実装時判断とする。 | FR-1, 6.2 | 既定はリクエスト送付方式。必要なら保存追加。 |
| P-4 | 確認待ち | Gemini 失敗時、ユーザー発話を保存するか否か。本設計では**失敗時は未保存（再送で確定保存）**を既定とする。 | FR-3, FR-5, E-2 | 異論あれば「失敗発話も保存」に変更可。 |
| P-5 | 前提 | Gemini呼び出しのタイムアウトを**既定30秒（GEMINI_TIMEOUT_MS=30000）**、**自動リトライなし**（FR-8の手動再送に委ねる）と設定。 | FR-8, 5.3, E-2 | 設定値として実装。 |
| P-6 | 前提 | 匿名IDの形式は `anon-` ＋ UUID等のランダム文字列とする（衝突回避）。要件は「匿名IDを生成」とのみ記載。 | FR-7 | 実装時に生成方式を確定。 |

---

## 11. 将来拡張（参考 / スコープ外）

> 要件定義書「12. 将来拡張」より転記。設計本体には含めない。

- アンケート結果／おすすめ企業との連携（パーソナライズ強化）
- 面接対策（模擬面接）・ESドラフト作成・企業比較・面接質問集
- ストリーミング表示、ログイン、本番デプロイ
- 岡山の地元企業データを文脈に組み込む（地域特化の寄り添い）
