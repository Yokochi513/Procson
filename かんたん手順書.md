# GitHub かんたん手順書（VS Code 版・今回やることだけ）

このプロジェクトに参加して作業を始めるまでの**最低限の手順**です。
編集・コミット・共有は **VS Code のボタン操作**で、最初の取り込み（クローン）だけ
**VS Code のターミナルでコマンド**を使います。上から順番にやればOK。

---

## やることは4つだけ

```
① 招待を受ける  →  ② VS Code と Git を入れる  →  ③ clone で取り込む  →  ④ 作業する
```

---

## ① 招待を受ける

1. GitHub から届いた招待メールを開く。
2. 青いボタン **「View invitation」** を押す。
3. ブラウザが開いたら（必要ならログインして）緑のボタン **「Accept invitation」** を押す。

![invitedされたことを知らせるメールの画面](<スクリーンショット 2026-06-15 215100.png>)

---

## ② VS Code と Git を入れる（初回だけ）

1. **VS Code**：https://code.visualstudio.com/ から「Download for Windows」→ インストール。
2. **Git**：https://git-scm.com/download/win から入手 → 選択肢は初期設定のまま「Next」でインストール。
3. インストール後、**VS Code を一度閉じて開き直す**。
4. VS Code 画面**上部に青文字で出る「サインイン」**をクリックして自分の GitHub アカウントにログイン（左下の**人型アイコン**からでもOK）。
5. VS Code 上部メニュー **「ターミナル」→「新しいターミナル」** を開き、最初に1回だけ：

```bash
git config --global user.name "あなたの名前"
git config --global user.email "登録したメールアドレス"
```

---

## ③ clone で取り込む（ターミナルでコマンド）

> VS Code は**既存のフォルダの中へクローンするのがUIでは難しい**ため、ターミナルで行います。

1. VS Code の **「ファイル」→「フォルダーを開く」** で取り込み先の親フォルダ（例：`C:\workspace`）を開く。
2. **「ターミナル」→「新しいターミナル」**（`Ctrl + @`）を開く。
3. GitHub のリポジトリページで緑の **「Code」** → **HTTPS** の URL をコピー。
4. ターミナルで実行：

```bash
# 取り込みたい場所へ移動（例）
cd C:\workspace

# クローン（URL は自分のものに置き換える）
git clone https://github.com/（オーナー名）/Games.git
```

> できた `Games` フォルダを「ファイル」→「フォルダーを開く」で開けば準備完了。

---

## ④ 作業する（毎回くり返す流れ）

VS Code 左端の**「ソース管理」アイコン（枝分かれマーク）**を使います。

```
作業前：プル（最新版を受け取る）
   ↓
ファイルを編集して保存
   ↓
ステージ（＋）＋ メッセージ入力 ＋ コミット（✓）
   ↓
同期（Sync Changes）で共有
```

操作の手順：

1. **始める前**：左下の**同期マーク（くるくる矢印）**を押して最新版を受け取る。
2. ファイルを編集して保存する。
3. **ソース管理アイコン**を開き、変更ファイルの **「＋」** でステージ。
4. 上の欄に**何をしたかメモ**を書いて **「✓ コミット」** を押す。
5. **「変更の同期（Sync Changes）」** ボタンを押してアップロード。

> **合言葉：「始める前にプル、終わったらコミット＆同期」**

---

困ったとき・くわしく知りたいときは [github使い方.md](github使い方.md) を見てください。
